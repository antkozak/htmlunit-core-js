This project is a Rhino fork, maintained to support features needed by HtmlUnit.

To build the project, you have to use ant (which then calls maven 2).

Notice that "org.mozilla.*" is automatically renamed to "net.sourceforge.htmlunit.corejs.*"

Run "ant -p -v" to see all the tasks available